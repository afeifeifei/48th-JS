alert(); 弹窗事件
console.log(); 控制体打印
confirm(); 点击确认按钮
prompt(); 输入框
document 文档对象
getElementById 通过ID来获取元素
onclick 单击点击
function 函数/方法
innerHTML 获取括号里面的内容

定义变量

let  声明定义变量的关键词  '使'

es5 中定义变量一般用  (var  function)
es6  ( let  conts 常量  function 函数/方法)

在es4中用es5规则命名定义变量,要么全部用es5的规则定义变量,不要混合来.
定义变量的时候,把变量名都统一的放在前面一起.
关于变量命名的规定 -不能使用关键词/保留词
组成变量的字符最好是 数字 字母 _ $ 四种 不要以数字开头
见名旨意
驼峰命名


JavaScript 的关键字和保留字大全
 关键字

具有特定用途
不能作为标识符
带 * 表示是 ECMAScript 第 5 版新增的关键字

break  do instanceof typeof case else new  var catch finally return void continue for
switch while debugger* function this with default if throw  delete in try

保留字

abstract enum int short boolean export interface static byte extends long super
char final native synchronized class float package throws const goto private transient debugger
implements protected volatile double import public
