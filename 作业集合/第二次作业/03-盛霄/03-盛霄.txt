定义变量 let 将相同事件整理在一起写，让代码更简洁
ES5 var function
ES6 let const function
变量名使用规定：不能使用关键词保留词（float）；不能使用js里面已经有意义的变量名；组成变量的字符最好是数字、字母、下划线、$，不能以数字开头；见名知意；驼峰命名法；
数据类型
数字；string字符串；``//模板字符串ES6中使用；boolean布尔值（true、false）；undefined未定义；null空；symbol ES6（独一无二的，通常对象属性）
若x后面要赋值可以用null
const不允许初始没有值
[]//数组 对象的一种，序号值从0开始
选取二维数组的方法
let x={};//对象 每一条用逗号隔开 属性：,
取值x.name